diff --git a/tools/building.py b/tools/building.py
index e078c1626c..77f4e2d5c 100644
--- a/tools/building.py
+++ b/tools/building.py
@@ -761,9 +761,11 @@ def DoBuilding(target, objects):
     # handle local group
     def local_group(group, objects):
         if 'LOCAL_CCFLAGS' in group or 'LOCAL_CPPPATH' in group or 'LOCAL_CPPDEFINES' in group or 'LOCAL_ASFLAGS' in group:
+            # Reason: SCons 4.x may return deque from Env.get(); coerce to
+            # list for safe concatenation with group's list values.
             CCFLAGS = Env.get('CCFLAGS', '') + group.get('LOCAL_CCFLAGS', '')
-            CPPPATH = Env.get('CPPPATH', ['']) + group.get('LOCAL_CPPPATH', [''])
-            CPPDEFINES = Env.get('CPPDEFINES', ['']) + group.get('LOCAL_CPPDEFINES', [''])
+            CPPPATH = list(Env.get('CPPPATH', [''])) + list(group.get('LOCAL_CPPPATH', ['']))
+            CPPDEFINES = list(Env.get('CPPDEFINES', [''])) + list(group.get('LOCAL_CPPDEFINES', ['']))
             ASFLAGS = Env.get('ASFLAGS', '') + group.get('LOCAL_ASFLAGS', '')
 
             for source in group['src']:
